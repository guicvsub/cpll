<Window x:Class="cpll.Views.MainWindowHost"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alunos"
        Height="640"
        Width="960"
        MinWidth="720"
        MinHeight="520"
        Background="{StaticResource Brush.Bg}">

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Alunos"
                       FontSize="26"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource Brush.Text}"/>
            <TextBlock Text="Cadastre, edite e organize os registros em um só lugar."
                       FontSize="14"
                       Foreground="{StaticResource Brush.Muted}"
                       Margin="0,6,0,0"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Formulário -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Dados do aluno"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource Brush.Text}"
                           Margin="0,0,0,16"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="160"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <TextBlock Text="NOME COMPLETO" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="TxtNome"
                                 Style="{StaticResource FormTextBox}"
                                 AutomationProperties.Name="Nome"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,16,0">
                        <TextBlock Text="E-MAIL" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="TxtEmail"
                                 Style="{StaticResource FormTextBox}"
                                 AutomationProperties.Name="Email"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="IDADE" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="TxtIdade"
                                 Style="{StaticResource FormTextBox}"
                                 AutomationProperties.Name="Idade"/>
                    </StackPanel>
                </Grid>

                <Separator Margin="0,24,0,20" Background="{StaticResource Brush.Border}" Height="1"/>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Inserir"
                            Style="{StaticResource BtnPrimary}"
                            MinWidth="120"
                            Margin="0,0,10,0"
                            Click="BtnInserir_Click"/>
                    <Button Content="Atualizar"
                            Style="{StaticResource BtnWarning}"
                            MinWidth="120"
                            Margin="0,0,10,0"
                            Click="BtnAtualizar_Click"/>
                    <Button Content="Excluir"
                            Style="{StaticResource BtnDanger}"
                            MinWidth="120"
                            Margin="0,0,10,0"
                            Click="BtnExcluir_Click"/>
                    <Button Content="Listar"
                            Style="{StaticResource BtnAccent}"
                            MinWidth="120"
                            Margin="0,0,10,0"
                            Click="BtnListar_Click"/>
                    <Button Content="Limpar"
                            Style="{StaticResource BtnGhost}"
                            MinWidth="100"
                            Click="BtnLimpar_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Grade -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Background="{StaticResource Brush.GridHeader}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="0,0,0,1"
                        CornerRadius="12,12,0,0"
                        Padding="20,14">
                    <TextBlock Text="Registros"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource Brush.Text}"/>
                </Border>

                <DataGrid x:Name="GridAlunos"
                          Grid.Row="1"
                          Margin="12,0,12,12"
                          AutoGenerateColumns="True"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="{StaticResource Brush.Border}"
                          BorderThickness="0"
                          Background="Transparent"
                          RowHeight="40"
                          AlternationCount="2"
                          RowBackground="White"
                          AlternatingRowBackground="#FAFBFC"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          CanUserResizeRows="False"
                          CanUserAddRows="False"
                          FontSize="13"
                          Foreground="{StaticResource Brush.Text}"
                          SelectionChanged="GridAlunos_SelectionChanged">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource Brush.GridHeader}"/>
                            <Setter Property="Foreground" Value="{StaticResource Brush.Muted}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="14,10"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="14,8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}"
                                                Background="{TemplateBinding Background}"
                                                BorderThickness="0">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#EEF2FF"/>
                                    <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
